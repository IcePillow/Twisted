package com.twisted.util;

public class Config {

    /**
     * If true, the zoom will be saved on a per-grid level in Game's SecViewport. If false, the
     * zoom will remain constant across all grids.
     */
    public static boolean isZoomGridSpecific(){
        return false;
    }

}
